{
  "name": "liquid-aerodrome",
  "license": "UNLICENSED",
  "scripts": {
    "format": "prettier --write .",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ liquid-aerodrome",
    "create-local": "graph create --node http://localhost:8020/ liquid-aerodrome",
    "remove-local": "graph remove --node http://localhost:8020/ liquid-aerodrome",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 liquid-aerodrome",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.82.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0",
    "prettier": "^3.1.0"
  }
}
